# コード監査メモ（2026-09-11、クラウドセッション）

`src/` 全体を読んで見つけた不具合と、今回の対応。ローカルの2つのセッション（「ゲットリッチアプリ開発」「ゲットリッチ アプリ再実装」）と
作業が重ならないよう、修正は**局所的な差し替え**に限定し、ブランチ `claude/quality-overseas-audit` に入れた。

## 今回直したもの

| # | 症状 | 場所 | 対応 |
|---|---|---|---|
| 1 | 世界旅行マスに着いたとき「オリンピック開催」の角名が出る | `4-game.js` travel 分岐 | `corners[2]`→`corners[3]` |
| 2 | マス選択の案内が「8秒」表示なのに締切は14秒 | `pickTile` | 案内を14秒に合わせ、文言に秒数を出す |
| 3 | サイコロ強化がゲームに効かない | `dieOf` | `dieLeveled()` を追加（Lvごとにゲージ枠＋1、ゾロ目＋0.4%） |
| 4 | サイコロ強化レベルが再読み込みで Lv1 に戻る（払ったゴールドが消える） | `5-meta.js fixSave` | レベルを 1〜30 に丸めて保持 |
| 5 | ペンダント強化が発動率に効かない | `pendFire` | 人間の持ち物のみ Lvごとに＋1% |
| 6 | 独占勝利の報酬倍率 `G.winX` が未使用 | `grantRewards` | 倍率を金額に反映し、報酬画面に「独占ボーナス ×N」を表示 |
| 7 | 祭り都市（×2）のバッジが描かれていない（コメントだけ） | `3-core.js drawTile` | 祭／五輪バッジを描画 |
| 8 | オンライン時の CPU が所持金以上に建てる／未所有地から1回でランドマーク | `7-online.js planBuild/olBuyUI` | オフライン `aiBuy` と同じガードを移植 |
| 9 | 本家にある攻撃系チャンスカードが無い | `CARDS` | 地震・疫病・都市チェンジ・招待状を追加（`pickEnemyCity` ヘルパ） |
| 10 | Windows 以外でビルドできない | `build.ps1` のみ | `build.mjs` を追加（出力は同一。マップのキー順だけ異なる） |
| 11 | 自動テストが無い | – | `tests/smoke.mjs`（Playwright）: 起動→対戦開始→数十ターン自動進行で JS エラー 0 を確認 |

## 2回目（本家寄せ）で追加したもの

| # | 内容 | 場所 |
|---|---|---|
| 12 | 本家「ワールド」マップ（台北→東京、観光地4つ、色8組）を既定マップに追加 | `3-core.js MAPS[0]`、`buildTiles` がマップごとの並び（slots/special/tours/prices）に対応 |
| 13 | 観光地タイル（`type:'tour'`）：購入のみ・建物不可・買収不可・攻撃カード対象外・通行料固定・観光地独占とライン独占の対象 | `tollOf / cityValue / assetOf / hasLine / monoOf / drawTile / buyUI / aiBuy / scoreLanding / aiPickTravel / olBuyUI / planBuild` |
| 14 | マップ選択で本家プリセット（30ターン・300万・25分）を自動反映。所持金に 300万／500万 を追加 | `renderMaps`、`2-body.html` |
| 15 | 給料・スタート到着ボーナスをマップ設定に（本家 30万／60万） | `salary`、スタート分岐 |
| 16 | 脱出カード・免税カード（アイテム＋チャンス） | `ITEMS / CARDS / useItem / jailTurn / 税務署分岐`。オンライン時の `olJailTurn` は未対応 |
| 17 | 固定金額の経済スケール `eco()` | チャンス賞金・修繕費・みんなから・ミニゲーム・オリンピック最低額・CPUの蓄え |
| 18 | 「監獄行き」→「無人島へ」など本家の呼び名に統一（角マス名はマップ依存） | `CARDS`、`2-body.html` |

## 3回目（ループ）で追加・削除したもの

| # | 内容 | 場所 |
|---|---|---|
| 19 | 削除: 効かない「キャラの絵柄」切替、待機部屋の「ゲーム友だち＋追加」、ペンダント／サイコロ画面の準備中タブ | `2-body.html`, `4-game.js`, `5-meta.js`, `8-dk.js` |
| 20 | カード画面に 売却／合成 を配線（到達不能だった機能の復活） | `8-dk.js showCards` |
| 21 | 悪魔カード（次の通行料を1回無料）・強制売却（建物1段を売らせる）。天使カードは本家どおり攻撃カードを自動で防ぐ防御カードに | `CARDS`, `angelBlock`, `payToll`, `drawTile` |
| 22 | クラス制: 入門／エコノミー300G／ビジネス1,000G／ファースト3,000G。参加費と賞金（1位70%・2位30%、ファーストは80/20） | `CLASSES`, `prizeFor`, `grantRewards`, 設定画面, 待機部屋見出し |
| 23 | 判断の制限時間: 購入・建設15秒、買収15秒、チャンス8秒、脱出カード10秒、サイコロ放置20秒で自動ロール。残り秒数を表示 | `modal`, `attachCountdown`, `buyUI`, `takeRoll` |
| 24 | チーム戦 2:2: 味方の街は通行料なし、独占は味方と合算、攻撃カードは味方を狙わない、相手チーム全滅／チーム総資産で勝敗、報酬は味方の勝ちでも勝利扱い。手番は🔴🔵交互 | `allied()`, `checkWin`, `timeUp`, `winnerName`, 設定画面 |

## 見つけたが今回は触っていないもの（ローカル側と要相談）

1. **`showCards` などが後勝ちで上書きされ、`5-meta.js` の実装が全部デッドコード**（`showHome/showCards/showDaily/showGacha/showPend/showQuest`）。
   売却・合成は `8-dk.js` 側に配線し直した（#20）。`5-meta.js` の死んだ画面（約40KB）はローカル側の作業と重なるので未削除。ロック機能は本家に無いので配線していない。
2. `raiseAlarm/alarmTick/sameMono/dropAlarm`（独占の猶予ターン）は誰からも呼ばれていない。使わないなら削除。
3. `#sideHud` を描く分岐があるが要素が無い。`'minigame'` タイル種別は生成されないのに3か所で分岐している。
4. `3c-chars.js / 3d-gems.js / 3f-anime.js / 3g-opm.js` がビルドから外れているのに、設定の「キャラの絵柄」切替 UI が残っている（切り替えても何も起きない）。
5. ランキング・勝利ティッカーは端末内の疑似データ。
6. `tiles[2].amount`（ボーナスマスの金額）、`SV.nick`、`SV.name`、`G.lastTick`、`p.cardLv` は未使用。
7. 画面のスクリーンショット確認では、待機部屋・順番決め・盤面ともに崩れは無し。外部フォント（Google Fonts）と PeerJS はネット遮断環境だと読めないが、フォールバックで動く。

## 動作確認

- `node build.mjs` → `index.html` / `game.html` 再生成
- `SMOKE_TURNS=40 npm run test:smoke` → JS エラー 0、対戦が進行、スクリーンショット `tests/out/`
- 新カード4種・サイコロ強化・セーブ正規化をページ内で直接呼び出して検証（すべて期待どおり）
